# Questions-CODEFORCES
These are the problems that i have solved in ***Codeforces*** using **JavaScript, C++**.

**I have also included some syntax for easy learning and quick reference**

// Use this syntax(for output use **console.log("")** and for input use let variable_name = input ) for **Javascript** and Select **Nodejs** while submitting on codeforces. 

```
const { log } = require("console");
const fs = require("fs");
const input = fs.readFileSync(0, "utf8").trim();
```

// **C++** SYNTAX
```
#define _ ios_base::sync_with_stdio(0);cin.tie(0);
#include <bits/stdc++.h>
#include <iostream>
using namespace std;
```